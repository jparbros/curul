.box
  .box-title
    %h2 Editar Representante

  .box-content  
    = form_for [:admin, @representative], :html => {:multipart => true, class: 'form-horizontal'} do |form|
      = render "admin/shared/error_messages", :target => @representative
      = render :partial => 'form', :locals => {:form => form}
      .form-actions
        = form.submit 'Editar Representante', :class => 'btn btn-primary'
        %input{:type => 'reset', :class => 'btn', :value => 'Limpiar'}