%tr
  %td= commission.name
  %td
    = link_to 'Editar', edit_admin_commission_path(commission), class: 'btn btn-primary btn-mini'
    = link_to 'Borrar', admin_commission_path(commission), :method => :delete, :confirm => '¿Esta seguro que desea eliminar la comision?', class: 'btn btn-danger btn-mini'