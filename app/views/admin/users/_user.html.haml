%tr
  %td= link_to user.email, admin_user_path(user) if user
  %td.center= image_tag is_admin_image user.admin? if user
  %td
    = link_to 'Editar', edit_admin_user_path(user), class: 'btn btn-primary btn-mini'
    = link_to 'Borrar', admin_user_path(user), :method => :delete, :confirm => '¿Esta seguro que desea eliminar la iniciativa?', class: 'btn btn-danger btn-mini'