.page-header
  %h2 Editar Usuario
= form_for [:admin, @admin], :html => {:multipart => true}, :method => :put do |form|
  = render "admin/shared/error_messages", :target => @admin
  %fieldset
    = render :partial => 'form', :locals => {:form => form}
    .actions
      = form.submit 'Editar Usuario', :class => 'btn primary'
      %input{:type => 'reset', :class => 'btn', :value => 'Limpiar'}