.page-header
  %h2 Nuevo Usuario
= form_for [:admin, @admin], :html => {:multipart => true} do |form|
  = render "admin/shared/error_messages", :target => @admin
  %fieldset
    = render :partial => 'form', :locals => {:form => form}
    .actions
      = form.submit 'Crear Usuario', :class => 'btn primary'
      %input{:type => 'reset', :class => 'btn', :value => 'Limpiar'}